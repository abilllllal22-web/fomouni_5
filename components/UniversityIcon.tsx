// ---------------------------------------------------------------------------
// v6 — простая универсальная иконка вуза (классическое здание с колоннами)
// вместо текстовых инициалов на квадратиках-монограммах. По запросу
// пользователя: единая узнаваемая иконка, а не буквы названия — при этом
// не претендует на официальный логотип конкретного вуза (демо-датасет,
// нет прав на реальные логотипы), просто нейтральный символ "университет".
// ---------------------------------------------------------------------------

export function UniversityIcon({ className }: { className?: string }) {
  return (
    <svg
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.8"
      strokeLinecap="round"
      strokeLinejoin="round"
      className={className ?? "h-5 w-5"}
      aria-hidden="true"
    >
      <path d="M12 3 3 9h18L12 3Z" />
      <path d="M5 9v9M12 9v9M19 9v9" />
      <path d="M3 21h18" />
    </svg>
  );
}
